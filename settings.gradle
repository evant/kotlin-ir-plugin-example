pluginManagement {
    repositories {
        jcenter()
        maven {
            url "file://$rootProject.projectDir/repo"
        }
    }
}

rootProject.name = 'kotlin-ir-plugin'
include 'sample-app'

